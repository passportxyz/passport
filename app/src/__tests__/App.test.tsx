// if you have not connected your wallet before, show home page
// if you have connected your wallet before, redirect to dashboard
/* eslint-disable-next-line @typescript-eslint/no-unused-vars */
import React from "react";

it("placeholder", () => {
  expect(true).toEqual(true);
});
