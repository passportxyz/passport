{
  "name": "infra",
  "scripts": {
    "pulumi:up:review": "APP_SECRETS=passport-review-secrets APP_ENV=passport-review-env op run --env-file='.env.pulumi' --  pulumi up",
    "pulumi:up:staging": "APP_SECRETS=passport-staging-secrets APP_ENV=passport-staging-env op run --env-file='.env.pulumi' --  pulumi up",
    "pulumi:up:prod": "APP_SECRETS=passport-prod-secrets APP_ENV=passport-prod-env op run --env-file='.env.pulumi' --  pulumi up"
  },
  "devDependencies": {
    "@types/node": "^14",
    "dotenv-cli": "^7.4.2"
  },
  "dependencies": {
    "@pulumi/aws": "^5.0.0",
    "@pulumi/awsx": "^0.40.0",
    "@pulumi/cloudflare": "^5.26.0",
    "@pulumi/github": "^6.1.0",
    "@pulumi/pulumi": "^3.0.0",
    "@pulumi/std": "^1.6.2"
  }
}
