{
  "name": "infra",
  "scripts": {
    "pulumi:up:review": "dotenv -e .env.review -- pulumi up",
    "pulumi:up:staging": "dotenv -e .env.staging -- pulumi up",
    "pulumi:up:prod": "dotenv -e .env.production -- pulumi up"
  },
  "devDependencies": {
    "@types/node": "^14",
    "dotenv-cli": "^7.4.2"
  },
  "dependencies": {
    "@pulumi/aws": "^5.0.0",
    "@pulumi/awsx": "^0.40.0",
    "@pulumi/cloudflare": "^5.26.0",
    "@pulumi/github": "^6.1.0",
    "@pulumi/pulumi": "^3.0.0",
    "@pulumi/std": "^1.6.2"
  }
}
